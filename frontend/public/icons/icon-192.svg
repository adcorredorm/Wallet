<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 192 192" width="192" height="192">
  <!-- PLACEHOLDER ICON — Replace with a real PNG before production release.
       Generate with: npx pwa-asset-generator logo.svg public/icons
       Colors match the project's dark theme from tailwind.config.js:
         background: #0f172a (dark.bg.primary / slate-900)
         accent:     #3b82f6 (accent.blue)
         text:       #f1f5f9 (dark.text.primary)
  -->
  <!-- Background -->
  <rect width="192" height="192" rx="24" fill="#0f172a"/>
  <!-- Wallet body -->
  <rect x="32" y="64" width="128" height="80" rx="10" fill="#1e293b" stroke="#334155" stroke-width="2"/>
  <!-- Wallet flap / card slot -->
  <rect x="32" y="64" width="128" height="28" rx="10" fill="#334155"/>
  <!-- Coin accent -->
  <circle cx="140" cy="104" r="18" fill="#3b82f6"/>
  <text x="140" y="110" text-anchor="middle" font-family="system-ui,sans-serif" font-size="16" font-weight="700" fill="#f1f5f9">$</text>
</svg>
